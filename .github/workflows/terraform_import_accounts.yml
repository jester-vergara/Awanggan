name: "Terraform Import"

on:
  push:
    branches:
      - main
  workflow_dispatch: # Allows the workflow to be triggered manually

jobs:
  plan-iam:
    runs-on: ubuntu-latest
    name: Terraform Import Bootstrap

    permissions:
      id-token: write
      contents: read

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Configure AWS credentials with OIDC
        uses: aws-actions/configure-aws-credentials@v2
        with:
          role-to-assume: ${{ secrets.AWS_ROLE_TO_ASSUME_CONTROL_TOWER }}
          aws-region: ${{ secrets.AWS_REGION }}

      - name: Set up Terraform Init Control Tower with Backend Config
        working-directory: ./aws/terraform/
        run: |
          terraform init \
            -backend-config="bucket=${{ vars.S3_BUCKET }}" \
            -backend-config="key=terraform/state/${{ vars.CONTROL_TOWER_ENVIRONMENT }}/${{ vars.CONTROL_TOWER_ENVIRONMENT }}.tfstate" \
            -backend-config="region=${{ vars.AWS_REGION }}" \
            -backend-config="dynamodb_table=${{ vars.DYNAMODB_TABLE }}" \
            -backend-config="encrypt=true" \
            -reconfigure


      - name: Create and Select Terraform Workspace
        working-directory: ./aws/terraform/
        run: |
          terraform workspace list | grep -q "control_tower" || terraform workspace new control_tower
          terraform workspace select control_tower

      - name: Terraform Plan create Control Tower resources
        working-directory: ./aws/terraform/
        run: |
          terraform plan -var-file="control-tower.tfvars" -out=tfplan

      # - name: Terraform Import Organization
      #   working-directory: ./aws/terraform/
      #   run: |
      #     echo "Importing AWS Organization"
      #     terraform import aws_organizations_organization.org o-ly1bfwh8zk

      # - name: Terraform Import Control Tower accounts
      #   working-directory: ./aws/terraform/
      #   run: |
      #     # Define the accounts and their IDs
      #     declare -A accounts=(
      #       ["Audit"]="920372985177"
      #       ["ControlTower"]="571600861891"
      #       ["Dev"]="448049827694"
      #       ["LogArchive"]="324037285298"
      #       ["Logging"]="977098986447"
      #       ["Prod"]="585768161105"
      #       ["QA"]="047719616326"
      #       ["Sandbox"]="774305606885"
      #       ["SecOps"]="050752649836"
      #       ["Shared"]="221082169697"
      #     )

      #     # Loop over each account and import
      #     for account_name in "${!accounts[@]}"; do
      #       account_id="${accounts[$account_name]}"
      #       echo "Importing account $account_name with ID $account_id"
      #       terraform import "module.aws_organizations_account[\"$account_name\"].aws_organizations_account.this" "$account_id"
      #     done


      # - name: Terraform Import Account
      #   working-directory: ./aws/terraform/
      #   run: |
      #     echo "Importing AWS account"
      #     terraform import 'module.aws_organizations_account["Audit"].aws_organizations_account.this' "920372985177"

      - name: Terraform Import KMS
        working-directory: ./aws/terraform/
        run: |
          echo "Importing AWS KMS Keys"
          # Import the S3 KMS key
          terraform import module.s3_kms_key.aws_kms_key.kms_key "arn:aws:kms:region:account-id:key/key-id"
          # Import the DynamoDB KMS key
          terraform import module.dynamodb_kms_key.aws_kms_key.kms_key "arn:aws:kms:region:account-id:key/key-id"
          # Import the alias for the S3 KMS key
          terraform import module.s3_kms_key.aws_kms_alias.kms_alias "alias/project-awanggan/awanggan-s3-kms-key"
          # Import the alias for the DynamoDB KMS key
          terraform import module.dynamodb_kms_key.aws_kms_alias.kms_alias "alias/project-awanggan/awanggan-dynamodb-kms-key"

