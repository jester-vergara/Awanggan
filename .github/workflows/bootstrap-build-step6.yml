name: "Bootstrap Build Step 5 - Build EKS"

on:
  workflow_dispatch: # Allows manual triggering, though it does nothing yet

jobs:
  placeholder:
    runs-on: ubuntu-latest
    steps:
      - name: Placeholder
        run: echo "This is a placeholder job."